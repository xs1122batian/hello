#include<stdio.h>
int main( )
{ 
int xs[10000];
int a,b,c,d,e,n;
e=0;
scanf( "%d",&n);
for( a=0;a<n;a++)
scanf( "%d",&xs[a]);
for( b=0;b<=n-1;b++)
{ d=0;
for( c=0;c<=n-1;c++)
{ if( xs[b]==xs[c])
d++;}
if( d==1)
e++;}
printf( "%d",e);
return 0;
}
