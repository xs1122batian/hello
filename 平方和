#include<stdio.h>
int sum(int n)
{
    int a,sum;
    sum=0;
    for(a=1;a<=n;a++)
    {
        sum=sum+a*a;
    }
    return sum;
}
int main()
{
   int n;
   scanf("%d", &n);
   printf("%d\n", sum(n));
   return 0;
}

