#include<stdio.h>
int main()
{
    int a,n;
    scanf("%d",&n);
    while(n++)
    {
        a=n%10;
        if(a==8)
        {
            printf("%d",n);
            break;
        }
    }
    return 0;
}
