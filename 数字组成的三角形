#include<stdio.h>
int main()
{
    int a,b,c,n;
    scanf("%d",&n);
    for(a=1; a<=n; a++)
    {
        c=1;
        for(b=1; b<=a; b++)
        {
            printf("%d",c);
            if(c==9)
                c=0;
            c++;
        }
        printf( "\n");
    }
    return 0;
}

