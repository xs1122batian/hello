#include<stdio.h>
int main( )
{ 
int i,a,n,c[10000];
int max, min,d;
scanf( "%d",&n);
for( i=0;i<n;i++)
scanf( "%d",&c[i]);
max=c[0];
min=c[0];
for(i=0;i<n;i++){ 
if(c[i]> max)
max=c[i];
if( c[i]<min)
min=c[i];}
d=max-min;
printf( "%d",d);
return 0;
}
