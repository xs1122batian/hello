#include<stdio.h>
int main( )
{ 
int xx[32];
int a,b,c,d,e,n;
for( a=0;a<32;a++)
scanf( "%1d",&xx[a]);
scanf( "%d",&n);
if( n==1)
printf( "%d",xx[0]);
if( n==2)
for( b=1;b<=8;b++)
printf( "%d",xx[b]);
if( n==3)
for( c=9;c<32;c++)
printf( "%d",xx[c]);
return 0;
}
